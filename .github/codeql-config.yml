name: "Ultra-Strict CodeQL"

# Only analyze main.py  
paths:
  - "main.py"

# Ignore everything else
paths-ignore:
  - "**/*"
  - "!main.py"

# Use only basic security queries
queries:
  - uses: security-and-quality

# Disable experimental features
disable-default-queries: false
